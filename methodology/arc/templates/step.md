# Arc Step

## Metadata

- Step ID: `step-01-<slug>`
- Title: `<short title>`
- Status: `planned`
- Owner: `shared`
- Created at: `YYYY-MM-DDTHH:MM:SSZ`
- Updated at: `YYYY-MM-DDTHH:MM:SSZ`

## Step Charter

- Goal: `<concise goal>`
- Done when: `<observable end condition>`
- Depends on: `(none)`
- Unlocks: `(none)`

## Constraint Envelope

- Write scope:
  - `` `src/<path>` - planned write surface ``
- Read-only scope:
  - `` `intents/system/<feature>/` - planned read surface ``
- Forbidden scope:
  - `(none)`
- Authority limits: `<authority boundaries or (none)>`
- External dependency posture: `none`
- Escalation triggers: `<trigger or (none)>`

## Capability Fit

- Context load: `medium`
- Novelty load: `low`
- Proof load: `medium`
- Edit breadth: `low`
- Review burden: `low`
- Sizing rationale: `<why this is one safe step>`

## Planned Layer Delta

- Intent: `(none)`
- Blueprint: `(none)`
- Code: `(none)`
- Descriptions: `(none)`
- Tests: `(none)`

## References

- Intent refs:
  - `` `intents/system/<feature>/feature.md` - touched intent artifact ``
- Blueprint refs:
  - `` `intents/blueprints/<blueprint>.md` - touched blueprint artifact ``
- Code refs:
  - `` `src/<path>` - touched implementation file ``
- Description refs:
  - `(none)`
- Test refs:
  - `(none)`
- Decision refs:
  - `(none)`

## Proof Target

- Intent targets:
  - `` `feature/<feature>#<kebab-case-outcome-id>` ``
  - `` `scenario/<scenario>#<kebab-case-check-id>` ``
  - `` `behavior/<behavior>#<kebab-case-check-id>` ``
- Required checks:
  - Success-path checks:
    - `<main success-path or contract check>`
  - Negative-path checks:
    - `(none)`
  - Invariant or regression checks:
    - `<must remain true>`
  - Review-only checks:
    - `(none)`
- Required evidence:
- Closure conditions:
- Failure triggers:

## Working Record

- Evidence run: `.methodologies/idd/scratch/evidence/<arc-id>/`
- Active evidence: `(none)`
- Current posture: `<what is true for the next pass>`
- Execution notes: `(not started)`
- Open questions: `(none)`

## Proof Packet

- Evidence refs: `(none)`
- Checks run:
  - Success-path checks:
    - `(not yet run)`
  - Negative-path checks:
    - `(not yet run)`
  - Invariant or regression checks:
    - `(not yet run)`
  - Review-only checks:
    - `(not yet run)`
- Results:
  - Success-path checks:
    - `(not yet run)`
  - Negative-path checks:
    - `(not yet run)`
  - Invariant or regression checks:
    - `(not yet run)`
  - Review-only checks:
    - `(not yet run)`
- Intent verdict: `not-applicable`
- Blueprint verdict: `not-applicable`
- Code verdict: `not-applicable`
- Residual gaps: `proof not yet run`
- Conflict and authority handling: `(none)`

## Review Packet

- Change summary: `(not yet run)`
- Artifacts changed: `(not yet run)`
- Reviewer focus: `(not yet run)`
- Downstream assumptions: `(not yet run)`
- Handoff summary: `(not yet run)`

## Outcome

- Decision: `continue`
- Reason:
- Next action:
- Step summary:

## Run Ledger

- `L01` | `YYYY-MM-DDTHH:MM:SSZ` | Operation: `Map Arc` | Status before: `planned` | Status after: `planned` | Inputs: `<summary or none>` | Changes: `<summary or none>` | Checks: `<summary or none>` | Evidence: `<evidence IDs or none>` | Next stage: `Shape Step` | Outcome: `<summary>`
  Detail: `<optional supporting detail>`
